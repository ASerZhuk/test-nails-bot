import Container from '@/app/components/Container'

import AppointmentClient from './AppointmentClient'

const page = async () => {
	return (
		<Container>
			<AppointmentClient />
		</Container>
	)
}

export default page
