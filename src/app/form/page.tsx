import Container from '@/app/components/Container'
import FormClient from './FormClient'

const page = async () => {
	return (
		<Container>
			<FormClient />
		</Container>
	)
}

export default page
