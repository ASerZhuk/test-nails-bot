import Container from '@/app/components/Container'

import ReservationForm from './ReservationForm'

const page = async () => {
	return (
		<Container>
			<ReservationForm />
		</Container>
	)
}

export default page
